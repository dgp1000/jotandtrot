{
  "name": "jotandtrot",
  "version": "1.0.0",
  "description": "Jot & Trot — collaborative trip planner",
  "private": true,
  "scripts": {
    "build": "python3 build.py && mkdir -p www && cp jotandtrot.html www/index.html",
    "ios": "npm run build && npx cap sync ios && node -e \"const f='ios/App/App/capacitor.config.json',j=require('fs');const c=JSON.parse(j.readFileSync(f));c.packageClassList=['AppleSignInPlugin','PushPlugin','SharePlugin','LinkPlugin','LiveActivityPlugin','CalendarPlugin'];j.writeFileSync(f,JSON.stringify(c,null,'\\t'))\""
  },
  "dependencies": {
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
